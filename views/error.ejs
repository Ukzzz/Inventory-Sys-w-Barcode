<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= error %> - Uniform Inventory System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full space-y-8 p-8">
        <div class="bg-white rounded-lg shadow-xl p-8 text-center">
            <!-- Error Icon -->
            <div class="mx-auto h-16 w-16 bg-red-100 rounded-full flex items-center justify-center mb-4">
                <i class="fas fa-exclamation-triangle text-red-600 text-2xl"></i>
            </div>
            
            <!-- Error Message -->
            <h2 class="text-2xl font-bold text-gray-900 mb-2"><%= error %></h2>
            <p class="text-gray-600 mb-6"><%= message %></p>
            
            <!-- Action Buttons -->
            <div class="space-y-3">
                <a href="/dashboard" class="w-full bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md font-medium transition duration-150 ease-in-out inline-block">
                    <i class="fas fa-home mr-2"></i>
                    Go to Dashboard
                </a>
                <a href="javascript:history.back()" class="w-full bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded-md font-medium transition duration-150 ease-in-out inline-block">
                    <i class="fas fa-arrow-left mr-2"></i>
                    Go Back
                </a>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="text-center text-gray-500">
            <p class="text-sm">&copy; 2024 Uniform Inventory System</p>
        </div>
    </div>
</body>
</html>
